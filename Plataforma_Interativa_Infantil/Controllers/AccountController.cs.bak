using Microsoft.AspNetCore.Mvc;

namespace backend.Controllers;

public class AccountController : Controller {
    [HttpPost]
    public IActionResult Login(string Email, string Senha, string Perfil) {
        // Demo: in production, validate credentials and redirect accordingly
        if (Perfil == "crianca") return RedirectToAction("CriancaDashboard","Home");
        if (Perfil == "pai") return RedirectToAction("PaiDashboard","Home");
        return RedirectToAction("ProfessorDashboard","Home");
    }
}
